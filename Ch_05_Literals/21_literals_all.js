let count = 42; // number literal
let negative = -5; // number literal
let zero = 0; // number literal

let hexadecimal = 0xFF; // hexadecimal number literal (255 in decimal)
let octal = 0o77; // octal number literal (63 in decimal)
let binary = 0b1010; // binary number literal (10 in decimal)